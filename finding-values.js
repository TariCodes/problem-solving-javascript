// let a function f be defined at an arbitrary point, x, by f(x)= 2*x**3 + 3.
//  Find the values of f at x = 1, 2, 3, 4, 5 and 6.

function f(x) {
    return 2 * x ** 3 + 3;
}
f(1), f(2), f(3), f(4), f(5), f(6);
